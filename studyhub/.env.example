DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/studyhub?schema=public"
STORAGE_DRIVER="local"
STORAGE_LOCAL_DIR="./storage"
MAX_UPLOAD_MB="50"
SEED_ADMIN_EMAIL="admin@studyhub.local"
SEED_ADMIN_PASSWORD="change-me"

# Lesson generation from handouts (optional). Get the key from platform.openai.com; never commit it.
OPENAI_API_KEY=""
OPENAI_MODEL=""
# Price per million tokens, so the admin screen can show what a run cost.
OPENAI_INPUT_PER_M=""
OPENAI_OUTPUT_PER_M=""
